import 'package:get/get.dart';

class HelperRegistrationBinding extends Bindings {
  @override
  void dependencies() {
    // Add registration controller here if needed
    // Get.lazyPut<HelperRegistrationController>(() => HelperRegistrationController());
  }
}
